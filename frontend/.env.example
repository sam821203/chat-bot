# API base URL (used at build time).
# Leave unset: in dev (npm run dev) uses http://localhost:10000;
#              in production build uses https://chat-bot-7ua2.onrender.com
# Set for deploy: e.g. VITE_API_BASE=https://your-api.onrender.com
# VITE_API_BASE=
